# 仓库维护约定

本文件适用于整个仓库，后续新增或修改代码时遵守以下约定。

## 中文注释

- 新增或修改关键逻辑时，同步补充简洁、准确的中文注释。
- 头文件重点说明类的职责、资源所有权、对象生命周期和线程约束。
- 实现文件重点说明设计原因、关键状态变化、回调关系、协议边界和异常分支。
- Linux 网络代码重点解释非阻塞 I/O、epoll 事件、Buffer 变化及连接关闭流程。
- 示例程序应说明从初始化、注册回调到进入事件循环的完整调用流程。
- 不给简单 getter、直接赋值等显然代码机械地逐行加注释。
- 修改代码行为时必须检查并同步更新已有注释，避免注释与实现不一致。

## 修改与验证

- 修改前先检查 Git 工作区状态，不覆盖已有改动。
- 修改后检查差异，确保没有混入无关文件。
- 按改动风险执行构建和相关示例验证，并说明未验证的部分。
